query → embedding → retrieval → prompt building → LLM → response formatting
Model weights
+ Tokenizer/config
+ Full inference pipeline
+ API server (FastAPI)
+ Pre/Post processing
+ Storage (DB)
+ Queue (for KIE)
+ Deployment (Docker)